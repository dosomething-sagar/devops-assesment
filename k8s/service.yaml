apiVersion: v1
kind: Service
metadata:
  name: devops-demo-service
  namespace: production
spec:
  selector:
    app: devops-demo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
